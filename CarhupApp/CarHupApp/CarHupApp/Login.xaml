<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarHupApp.Login"
              BackgroundColor="#003">
             
    

    <ContentPage.Resources>
        <!-- Estilo para Entry -->
        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="White" />
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="FontFamily" Value="Century Gothic" />
            <Setter Property="FontAttributes" Value="Bold" />
        </Style>


        <!-- Estilo para Button -->
        <Style TargetType="Button">
            <Setter Property="TextColor" Value="White" />
            <Setter Property="CornerRadius" Value="15" />
            <Setter Property="FontFamily" Value="Century Gothic" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="BorderColor" Value="White" />
        </Style>
    </ContentPage.Resources>
    

    <ContentPage.Content>

        <!-- Estilo para Login -->
        <StackLayout Padding="20" VerticalOptions="End">


            <Image Source="CarHupLogoApp.png" Scale="1.2"/>

            <Entry x:Name ="Usuario_T"  Placeholder="Usuario" Margin="0,0,0,20">
            </Entry>

            <Entry x:Name="Passaword_T"  Placeholder="Contraseña" IsPassword="True" Margin="0,0,0,34">
            </Entry>

            <Button Text="Acceder" Margin="0,0,0,40" VerticalOptions="Center" Clicked="IniciarSesion">
            </Button>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>